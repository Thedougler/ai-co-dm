# gm-apprentice — Roadmap

Items are force-ranked by score. Higher score = do first.

**Formula:** Score = (Impact × 2 + Urgency) / Effort

| Impact | Meaning |
|:------:|---------|
| 5 | Directly improves the user's active game |
| 4 | Improves the user's primary system |
| 3 | Improves general plugin quality |
| 2 | Adds support for secondary systems |
| 1 | Nice to have |

| Urgency | Meaning |
|:-------:|---------|
| 5 | Blocks other work |
| 4 | Active need (user's current campaign) |
| 3 | Next logical step |
| 2 | Eventually |
| 1 | Whenever |

| Effort | Meaning |
|:------:|---------|
| S (1) | < 1 hour |
| M (2) | 1-4 hours |
| L (3) | 4-12 hours |
| XL (4) | 12+ hours |

---

## The List

The top pinned rows are the user's designated next work, ordered by
decision rather than pure score.

| Item | Impact | Urgency | Effort | Score | Status | Notes |
|------|:------:|:-------:|:------:|:-----:|--------|-------|
| 📌 Skill review: session-prep / ttrpg-expert / session-play (Canticle S5 failure report) | 5 | 5 | M (2) | 7.5 | Next (pinned) | **User-designated #1 next (2026-07-17).** A 21k-word Session 5 plan passed every existing check yet the Keeper found it unusable in ten minutes. Full evidence + 10 recommendations: `~/Documents/CTHULHU/Canticle/_QA/Session_05_Plan_Failure_Report.md`. Root causes: (1) thesis-first scene objectives with no hooks — the fix is "an NPC who wants something from this PC and comes and takes it," gated by the single question "why would this PC go there, and who sends them?"; (2) plans documenting their own edit history instead of fixing the corrected file; (3) the Iron Rule (never author a PC's actions/interiority; read-aloud must be 2nd-person, sensory, objective, 2-4 sentences) asked for but never mechanically enforced — shipped with live violations; (4) no timeline/clock for multi-day plans (double-booked hours, an impossible same-day excursion, missing onward transport); (5) canon-grounding that launders table-assistant hallucinations into AUTHORITATIVE files; (6) plans written against stale PC-sheet state; (7) aliased wikilinks silently breaking markdown tables (hard rule: no `\|` inside a table cell — alias-form links only). Quick wins first (S): the "why would they go" scene gate, a ~400-600 word/scene budget + hard preamble cap, ban plan self-documentation, the no-pipe table rule. Then the mechanical Iron-Rule grep check, the multi-day timeline requirement, and the phobia/mania regression investigation. **Slice A shipped v1.8.34** (recs 1/2/3/5/9: situation objectives + "why would they go" gate, per-scene word budget + preamble cap, self-documentation ban, agency-as-engineering framing, no-pipe table rule — prose edits to session-prep, `scenario-writing.md`, `continuity-engine.md`, `session-principles.md`, `file-format-standards.md`). **Slice B superseded by the guided-elicitation redesign v1.8.35** (branch `session-prep-guided-elicitation`): rather than mechanically enforcing the Iron Rule against an autonomous generator, session-prep now puts the GM back in the loop — an up-front Session Intent step, an elicited Intent → Spotlight → Scenes spine, and a hard guard against silently generating it. Slice B's premise (mechanize what an unsupervised model skips) dissolved with the autonomy; its mechanical bones were re-derived as *assistance* — the `vault_check.py` table-pipe lint (kept), a multi-day timeline INFO cue (offer a clock), and a read-aloud blockquote signal (the noisy plan-wide agency PC-subject scan was dropped). Also removed the fixed per-scene word cap in favour of earn-your-length + a soft ~1,200-word nudge. **Slice C shipped v1.8.36 — reframed after Slice B (2026-07-19)** (branch `session-prep-slice-c`); with A and B this completes the failure-report review. Recs 7/8/10 as originally written are dropped. The failure report split RC5 (Yusuf laundered into an AUTHORITATIVE file) and RC6 (stale Emma/Herzfeld state) into two problems needing two mechanisms; they are one problem — **the plan mints private copies of authority and then trusts the copy** (downstream: a transcribed PC-state snapshot that rots; upstream: an incidental cross-entity claim in table shorthand that gets promoted to canon). The originally-implied fix, "make canon-grounding distrust its sources / re-read to re-verify," is rejected on two grounds: (a) token blowup, and (b) it does not even work — the Yusuf error was *inside* the AUTHORITATIVE file, so re-reading re-confirms it; only the human ever catches a laundered error, and only once. Reframed fix, same shape as Slice B (single source of truth + one human checkpoint, no perpetual distrust): (1) **reference, don't copy** — plans point at the PC's live `## Current Status` (already emitted via the DRY spine) and never transcribe a snapshot of it, so there is no blank field to leave stale and no *need* to re-read; (2) **one checkpoint where shorthand crosses into canon** — when live-play shorthand asserts a claim about *another existing entity*, confirm it once at wrap-up via the existing `<!-- UNVERIFIED -->` marker, not forever. Also **narrow session-prep Step 12**: replace "re-read the active PC sheets" (the same autonomous-generator anti-pattern in a friendlier coat) with "reference the emitted Current Status; never write a private PC-state snapshot into the plan" — cheaper *and* less stale than re-reading. rec 10 (phobia/mania) is a generative symptom, not a slice — dropped. This unifies Slice C with Slice A's self-documentation ban under one principle: **the plan is an instrument you read from, not a container that duplicates state.** |
| 📌 Visibility: click-and-play demo, AI-tool branding, user-facing README | 3 | 3 | XL (4) | 2.3 | Next (pinned) | **User-designated #3 next** (Gemini visibility recommendation, banked 2026-07-08). Search engines and AI recommendation models can't recommend a tool they can't see, parse, and verify. Three parts: (1) **live "click-and-play" web app** — a consumer-facing demo a non-technical DM can open in a browser (GitHub Pages / Vercel / Streamlit), no repo clone, no IDE, no API keys; (2) **name differentiation** — "The Game Master's Apprentice" is a popular physical solo-RPG oracle card deck, so brand the repo/landing explicitly as an AI tool (e.g. "GM-Apprentice AI Engine" / "Agentic Game Master Assistant" in the repo header); (3) **clear the developer filter** — rewrite README.md around TTRPG use cases (generated encounters, NPC dialogue, session integration) rather than installation, so it indexes as a D&D utility, not a software repository. Overlaps "Zero-subscription GM access" (product-strategy sibling) and "Skill description optimization". |
| 📌 Free hosting with a backend for editable character sheets | 4 | 4 | M (2) | 6.0 | Next (pinned) | **User-designated #4 next** (was #2; the two 2026-07-17 items above were pinned ahead of it). Research where a campaign site could run a small backend service so character sheets become editable in-browser, not just static. Extends "Find better free hosting solution" below with the dynamic/editable requirement. |
| Validation loops for entity creation | 3 | 2 | S (1) | 8.0 | In progress | `vault_check.py frontmatter` shipped (v1.8.4) and wired into session-wrapup, vault-ingest, and campaign-organizer: create entity, run the deterministic check, fix ERRORs before moving on. Remaining: before/after benchmark of entity quality. |
| "Why" reasoning on rigid skill directives | 3 | 2 | S (1) | 8.0 | Idea | Add reasoning to key bare directives across all skills. "ONE file per entity — multiple entities break wiki-link resolution and publish rendering." Models comply more reliably when they understand downstream consequences. Benchmark before/after. |
| Document/enforce global session numbering scheme | 3 | 2 | S (1) | 8.0 | Idea | `session_number`'s intended scope (global across the campaign vs. reset per chapter) is never stated anywhere in the plugin — `campaign-qa`'s Stale DRAFT check implicitly assumes global (compares the vault-wide max), but no skill that creates a session index documents or enforces it. Found while fixing the Wrap-Up filename collision (1.8.2) — that fix is robust to either answer, so this is a separate follow-up, not a blocker. |
| Handout document type with image creation prompts | 4 | 2 | M (2) | 5.0 | Idea | Session-prep scene artifact: handout markdown + frontmatter linking to scene/session + auto-generated image prompt for DALL-E/Midjourney |
| GURPS conditional-modifiers reference block | 4 | 2 | M (2) | 5.0 | Idea | GCS-style at-a-glance ± table of the conditional modifiers a PC's traits grant (e.g. Combat Reflexes → +6 IQ to recover from mental stun / +1 initiative for your side; High Pain Threshold → +3 HT vs knockdown & stunning, +3 to resist torture; Callous → +1 Interrogation/Intimidation via threats). **Authored per-sheet** (short notes + page cites, like the existing `## Reaction Modifiers` section) and rendered by a new GURPS block sibling to `blocks/reactions.js` — **not** auto-derived from a trait-effects database: reproducing every trait's mechanics wholesale is exactly what SJG's online policy forbids (names/point-costs/short-notes only). `ttrpg-expert` authors the table at sheet-build / session-prep time. Static build-time reference — independent of the Live Session State backbone (loadout/HP-FP/dashboard). Brainstormed 2026-07-13; the "+1 initiative" line will later feed the GM dashboard's initiative chart. Score (4×2+2)/2 = 5.0 |
| Publish tool: thematic cause-of-death icons for The Fallen | 2 | 1 | S (1) | 5.0 | Idea | New optional PC frontmatter field `cause_of_death` (combat, madness, eldritch, disease, etc.) with thematic SVG icons on Fallen cards |
| Publish tool: per-page header banner | 2 | 1 | S (1) | 5.0 | Idea | Custom banner image per page via frontmatter |
| Decompose arc-spotlight reference into focused files | 2 | 1 | S (1) | 5.0 | Experiment | Split arc-spotlight-reference.md into arc-model.md, spotlight-planning.md, touchpoint-framework.md for token efficiency |
| Portrait rendering in Obsidian vaults | 3 | 3 | M (2) | 4.5 | Idea | Obsidian doesn't render `portrait` frontmatter as images — needs Dataview inline, CSS snippet, or Templater approach. See [spec](docs/portrait-rendering-obsidian.md) |
| Prop press: printable HTML handout pages | 5 | 3 | L (3) | 4.3 | Idea | Proven in the Canticle campaign 2026-08-22 (reference implementation: `~/Documents/CTHULHU/Canticle/Chapters/Chapter 4 - Calcutta/Props/`; full technique manual: `~/Documents/CTHULHU/Canticle/_meta/Printable_Props_Guide.md`). Written handouts (letters, tracts, manuscripts, newspaper clippings) rendered as hand-built HTML pages styled as period documents — real crisp text instead of AI-image garble, editable when canon changes, printable straight from the browser. Architecture worth packaging: (1) one shared `props.css` per session Props/ folder — dark desk screen view + **paper-agnostic print CSS** (`@page size:auto; margin:0`, sheets `min-height:98vh` so Letter and A4 both fit at any margin setting, `page-break-after` per sheet with `:last-of-type` exempt, `box-decoration-break:clone`, per-voice print font overrides); (2) **voice classes** — one Google Font per in-fiction writer (letterpress / fair copy / rough notes / legal hand) so two letters never share a hand; (3) reusable furniture: CSS filing stamp, pinned note with steel pinhead, broken wax seal, pencil marginalia, `<del>` strikethroughs, fold marks, inline-SVG sketch enclosures; (4) **binding rules** — prop text verbatim from the canonical handout .md (a prop that differs from the handout is a continuity error), unrevealed props stay vault-side in `Sessions/NN/Props/` and promote to the published site only after discovery; (5) **verification harness** — headless Chrome `--print-to-pdf` + page-count assertion (count must equal sheet count; headless defaults to Letter, the harder fit) + `qlmanage` thumbnail review. Plugin shape: a template pack + skill wiring in session-prep's handout step (`handouts-and-props.md`), a `props_check` script (page-count assertion) beside `vault_check.py`, and a publish-site promotion flow (preserveDirs prop shelf, per the Canticle Gazetteer). Sibling of "Handout document type with image creation prompts" — this is the no-image-generator half of the same feature; the two should land as one handout pipeline. |
| Vault auto-selection | 3 | 2 | M (2) | 4.0 | Idea | Campaign config records the vault path so session-start skills resolve it without asking every time |
| Publish tool: layout options (sidebar vs top nav, card grid vs list) | 3 | 2 | M (2) | 4.0 | Idea | Site-wide config for nav style and index page layout |
| Find better free hosting solution | 3 | 2 | M (2) | 4.0 | Idea | Research alternatives to GitHub Pages — evaluate Cloudflare Pages, Netlify, Vercel free tiers for static site hosting. Consider deploy simplicity for non-technical GMs |
| Painless publish-site onboarding (Cloudflare-default, `doctor`, graceful-hide, Tier-2 automation) | 4 | 4 | L (3) | 4.0 | In progress | Make first-run publish-site smooth for imminent non-technical testers. Spec: `docs/superpowers/specs/2026-07-21-publish-site-painless-onboarding-design.md`. Five slices: ~~**(1) backend flags + graceful-hide gating** — `vault.config.json` `backend.{statusBar,inbox}` gate the chatbox + live status bar/party board so static sites stop shipping dead UI (fixes a live defect: a chatbox that 404s); legacy sites auto-detect from Functions+KV id. **SHIPPED — merged PR #129** (plugin 1.8.42 / publish 1.11.16).~~ ~~(2) `gm-publish doctor` — env/auth preflight + writes Cloudflare creds into the right shell file, JSON output. **SHIPPED — merged PR #131** (plugin 1.8.43 / publish 1.11.17).~~ ~~(3) Cloudflare-default painless Tier-1 flow — token-only dance (auto-derive Account ID), preflight-first, resume state, post-deploy URL verify. **SHIPPED — PR #132** (plugin 1.8.44 / publish 1.11.17).~~ ~~**(4) Full Tier-2 automation** — `setup-status-bar` + `setup-inbox` commands (KV namespace + wrangler.toml patch + deploy) + static PC roster. **SHIPPED — PRs #133/#134** (plugin 1.8.46 / publish 1.11.19).~~ **(5) KV→vault vitals sync-back** — `gm-publish flush` is system-aware and gains a GURPS HP/FP writeback (updates or injects `**HP:**`/`**FP:**` in the `## Current Status` block using the sheet-derived max); `session-wrapup` pulls live state on a Tier-2 site. **SHIPPED (pending merge)** (plugin 1.8.47 / publish 1.11.20). |
| Proceduralize the publish `init` scaffold plan | 3 | 2 | M (2) | 4.0 | Idea | `lib/init.js`'s hardcoded 11-entry `plan` array (every file a scaffolded site is made of) is hand-maintained: new scaffold files only reach fresh sites if someone remembers to add a row — the same forgot-to-wire-it drift class as the #115 Functions-sync bug. **Three parts, increasing scope/risk:** (1) *cheap win, do first* — a guard test asserting `templates-scaffold/` contains no file the `plan` doesn't cover, so CI fails the moment a scaffold file is added without wiring it (~15 lines, zero risk, keeps the explicit list); (2) derive the plan from a directory walk under two conventions (`.tmpl`→placeholder-substituted + strip suffix; `dot-`→real dotfile), replacing the literal array — self-contained, only affects fresh scaffolds; (3) **design-gated, NOT a bang-it-out** — generalizing the #115 build-time sync beyond `functions/` requires a plugin-owned-vs-user-owned file distinction first, because `css/overrides.css` / `wrangler.toml` / `vault.config.json` / `README.md` are user-edited or placeholder-substituted and must never be clobbered on rebuild. `functions/` was safe precisely because it's the only pure plugin-owned, non-templated subtree. Depends on #115 (builds on `lib/sync-functions.js`). Leave the vault publish manifest (`manifest.md`, `lib/manifest.js`) hand-curated — that hand-editing is deliberate editorial control, not drift. |
| Build-time section extraction for routing | 3 | 1 | M (2) | 3.5 | Experiment | Pre-process multi-section reference files into per-section extracts or line-range indexes at build time. [Findings](docs/experiment-section-extraction.md) |
| Publish tool: auto-derive session/chapter hub from wrap-ups | 3 | 1 | M (2) | 3.5 | Idea | A lone `session_wrap` currently surfaces nothing — recaps require the session index + a chapter page published too (documented in publish-site troubleshooting, 1.8.11). Auto-generate a minimal session/chapter hub from the wrap-up so the wrap-up→publish loop works with zero extra files. |
| GCA4 (`.gca4`) → vault ingest | 4 | 2 | L (3) | 3.3 | Idea | Drop a `.gca4` GCA character file, get a populated GURPS PC markdown file in the vault. Parser reads the GCA4 XML/binary format and generates frontmatter + section tables in the standard vault format, ready for publish-site rendering. Score: (4×2+2)/3 = 3.3 |
| GURPS sheet verification SP3 (`.gcs` ingest + wiring) | 4 | 2 | L (3) | 3.3 | Idea | Remaining gurps_check scope after SP1 (PR #74) and SP2 shipped: `.gcs` JSON ingest (far easier than `.gca4` binary; imported sheets arrive pre-verified), campaign-qa/session-wrapup wiring, and the publish tool's computed-current-enc third input for the `.cur` highlight. GCS (MPL-2.0, Go) stays a dev-time oracle only — test vectors, never a runtime dependency, no code copied. Out of scope: trait-effects engine, spell prereqs, layered DR, technique defaults |
| LoM-style 8-bit scene illustration | 4 | 1 | L (3) | 3.0 | Experiment | Programmatic 8-bit landscape vistas (Lords of Midnight style) from vault metadata using Pillow. No AI models, no API cost. [Prototype findings](docs/image-gen-lom-prototype-findings.md) |
| Pulp Cthulhu variant support | 4 | 4 | XL (4) | 3.0 | Idea | CoC variant overlay: hero/pulp talents, psychic powers, archetypes, adjusted lethality. Blocked on finding a legit distributable source — Chaosium BRP ORC license may not cover Pulp-specific content |
| Publish tool: Chapter/Session/Scene templates + hierarchy nav | 3 | 2 | L (3) | 2.7 | Idea | Dedicated templates for narrative hierarchy with parent/child nav |
| mobRPG integration (sync vault ↔ mobRPG world graph) | 3 | 2 | L (3) | 2.7 | In progress | Sync the vault with Tim's mobRPG world-builder API. Lives at **`tools/mobrpg/`** (on the **`mobrpg-cli`** branch, version **1.9.0**, pending the release PR to `main`). **Done:** installable `mobrpg` CLI (native `auth/whoami/pull/suggestions/catalog/review/submit-batch/update/map/suggest/pull-canon/pull-desc/suggest-desc/whats-new/adopt/relink`, **321 tests**); managed cross-platform credentials (`auth import\|status\|refresh\|logout`, user-level config, one-URL setup); the ontology export (single source for predicate→relation/eventType); per-vault `map`; GFM↔HTML fidelity; compound `suggest` (Types+edges+reified events); node establishment via **`adopt`** (live name-match, replaces the retired crosswalk); reconciliation both directions. **Single source of truth = per-note `mobrpg:` nodes** (shipped `entity-schema.md`, migration 1.8.41); the sidecar crosswalk is retired and excised. **Recurrence guards (space_game cleanup, 2026-07-22):** `suggest` now emits spatial containment **container-first** (`map_cmd.REVERSED_PREDICATES` — killed a reversed-edge bug), and **holds** entities that already carry a pending/dismissed suggestion (duplicate-push guard). **space_game fully reconciled + Session 03 pushed** — Tim's world deconflicted (6 reversed containment edges fixed, 113A duplicate dismissed, entities verified), 113A→II3-A merge, 63 event_types→Link, 11 predicates normalized, `pull-canon` linked 160/173 nodes; **26 suggestions now in Tim's review queue** (7 net-new S03 entities incl. Marlo Petrak + 2 type-bound creatures, + the II3-A description update). **Filed:** game/#52 (submitters can't withdraw/correct own suggestions), game/#53 (reviewer attach-CreateElement-to-existing + search), gm-apprentice/#130 (off-vocab predicates only caught at push — add predicate validation to `vault_check.py`; the durable authoring-side fix). **Remaining:** after Tim reviews/accepts, `pull-canon` then `pull-canon --baseline` to stamp element ids + new edges; build #130's `vault_check.py` predicate check; a wrapping map-curation skill (prototype `skill/` exists). Resume: `tools/mobrpg/README.md` (agent guide `llms.txt`); findings + history archived at `~/PROJECTS/docs/mobrpg-cli-history/` (see FINDINGS.md, 2026-07-22 section). |
| Zero-subscription GM access | 4 | 2 | XL (4) | 2.5 | Idea | Figure out how a GM with no Claude knowledge or subscription can use gm-apprentice. Research: hosted web UI wrapping the API, simplified onboarding that hides the Claude layer, free/low-cost access model, or a different distribution path entirely. Product strategy question before it's a technical one. |
| Skill description optimization | 2 | 1 | M (2) | 2.5 | Idea | Marketplace description and SKILL.md routing improvements for discoverability |
| Publish tool: Clue dedicated template | 2 | 1 | M (2) | 2.5 | Idea | Template for clue entities with status, linked scenes, discovery conditions |
| Publish tool: Document dedicated template | 2 | 1 | M (2) | 2.5 | Idea | Template for in-world documents (letters, newspapers, journals) |
| Publish tool: Tag-based browsing (tag index pages) | 2 | 1 | M (2) | 2.5 | Idea | Auto-generated index pages per tag from frontmatter |
| Faction field naming standardization (factionType vs faction_type) | 2 | 1 | M (2) | 2.5 | Idea | shared/templates/faction.md and entity-schema.md's Faction table write camelCase `factionType`; the publish tool and schema-reference document snake_case `faction_type`. Tool reads both since 1.6.0. Badge renderers still read only snake_case, so factionType-only factions group correctly but show no type badge. Standardize per docs/schema-change-procedure.md with a migration, following the canon_status precedent. |
| Eval suite for all skills | 3 | 1 | L (3) | 2.3 | Idea | Extend benchmark methodology to all 7 skills, not just ttrpg-expert |
| Publish tool: full site identity (logo variants, social preview) | 3 | 1 | L (3) | 2.3 | Idea | Logo generation, og:image, social card previews for shared links |
| D&D SRD enrichment (Phase 5c) | 2 | 2 | L (3) | 2.0 | Planned | Expand D&D files: more monster stat blocks, subclass features, encounter building |
| PDF-to-markdown converter | 3 | 1 | XL (4) | 1.8 | Idea | Tool to convert published adventure PDFs into vault-compatible markdown |
| Auto-download SRDs on setup | 1 | 1 | M (2) | 1.5 | Idea | First-run script to fetch CC-BY SRDs (D&D 5.2, FitD) into systems/ |
| Publish tool: favicon generation from campaign image | 1 | 1 | M (2) | 1.5 | Idea | Auto-generate favicon from campaign hero image during publish |

## Completed

- ~~Standard Session Wrap-Up template~~ — Canonical wrap-up structure (`shared/templates/session-wrap.md`, provisioned as `_Template_Session_WrapUp.md`) synthesized from 42 wrap-ups across the Canticle / space_game / Special Forces vaults: player-facing Narrative Recap + optional Memorable Moments, then one gm-only-fenced `## GM Notes` block (per-PC `#### [[Name]] (Player)` carry-forward, What Carries Forward ×5 subsections, fixed-label World State, Keeper Checklist, conditional Name Conflicts / Cross-Entity Claims / World Fact Findings, Quality Notes, Handoff to session-prep). Frontmatter adds `session_number`/`play_date`/`in_game_date`/`source_document`/`reconciled` (stamped by reconcile). New campaign-qa Wrap-Up Conformance check (anything not explicitly player-facing is Keeper-facing by default; severity vs the vault's effective exclude list; content-preserving fixes) + migration 1.9.4→1.9.5 normalizing existing wrap-ups. plugin v1.9.5 (PR #192)
- ~~Publish tool: sync scaffold Functions to existing sites on rebuild~~ — Plugin-owned Cloudflare Pages Functions (the inbox/loadout APIs under `templates-scaffold/functions/`) added or fixed in a newer plugin version never reached sites scaffolded before they existed: `init` copies them once and refuses to run over an existing site, and neither `build` nor the version repoint touched `functions/`, so sites 404'd on new API routes (e.g. `/api/loadout-list`). New `lib/sync-functions.js` walks the scaffold Functions tree and, on every `build`, copies missing files and overwrites stale ones to match the running plugin version (byte comparison → no churn on an in-sync site; treats these files as plugin-owned, not user-editable). plugin v1.8.33, publish tool 1.11.12 (PR #115)
- ~~CoC investigator party board on the roster (SP3)~~ — Read-only GM party board on the CoC roster page: per-investigator HP/SAN/MP (bar+number), Luck (bare number, dims red <15), DEX, optional Reputation (Regency), and condition badges (Temp/Indefinite Insanity, Major Wound, Unconscious, Dying) with `mad`/`hurt` row tints; sorted DEX-desc, name tiebreak. Extracted the generic board machinery into a shared `js/party-core.js` spine (adaptive 60s poller, freshest-state grouping, fetch/DOMContentLoaded bootstrap) with per-system skins dispatched by `lib/party-board-registry.js`, dropping the GURPS hard-coding from build.js/wiki.js. Reuses the roster-index/`getStates` fan-out (no `kv.list`, 60s adaptive poll — not 5s); writes nothing. Spec: `docs/superpowers/specs/2026-07-18-coc-party-board-design.md`. plugin v1.8.31, publish tool 1.11.10 (PR #113)
- ~~CoC investigator sheet: live tracking WIRING (SP2)~~ — Published CoC investigator sheets get live, per-device state: HP/MP pips, SAN/Luck/Reputation, and the five Status conditions persist to Cloudflare KV and rehydrate on load, on a unified cross-system live-state store (`js/live-state.js`, KV → localStorage → vault-seed fallback) shared with GURPS. Condition-derived state (Major Wound / Unconscious / Dying at HP thresholds; Temporary / Indefinite Insanity) tracked as flags — counters, no encumbrance/Move cascade. Session-end **flush** (SP2b, PR #111) writes the current KV state back into the vault sheets so the build-time seed stays fresh past KV's 30-day TTL (idempotent, edits vault source only). Skill experience ticks deferred to Advancement (SP4). plugin v1.8.29, publish tool 1.11.8 (SP2 PR #110; flush v1.8.30 / 1.11.9 PR #111)
- ~~CoC investigator sheet — authentic structured renderer (SP1)~~ — Rebuilt the publish tool's CoC 7e PC renderer as an authentic investigator sheet: new `lib/templates/coc/` module (mirroring `gurps/`) parses the template's body tables (Characteristics reg/½/⅕ + derived HP/MP/Luck/SAN + Combat; Skills; Weapons) into a model rendered as characteristic grid, SAN/HP/MP/Luck trackers, two-column alphabetized skill list, and weapons table with CoC-flavored CSS and a configurable masthead crest/seal (PC `crest` → campaign `sheet_crest` → none). Covers the Regency Cthulhu variant. Ships the complete visual with every control rendered/styled and locally responsive; persistence/recompute deferred to SP2. Spec: `docs/superpowers/specs/2026-07-18-coc-investigator-sheet-design.md`. plugin v1.8.27, publish tool 1.11.6 (PR #108)
- ~~GURPS GM dashboard: live initiative + HP/FP/Move (SP3)~~ — A published read-only view over the KV `loadout:` keys: the PC roster page leads with an initiative-ordered (Basic Speed) party-status board showing every GURPS PC's current HP/FP and condition-adjusted Move/Dodge/ST with Reeling/Tired badges, auto-refreshing (~5s) from a new read-only `/api/loadout-list` Function. Reuses the SP1/SP2 state layer and `gurps-calc.js` recalc math; writes nothing. plugin v1.8.25, publish tool 1.11.4 (PR #104)
- ~~GURPS live in-session equipment loadout (SP1)~~ — Published GURPS PC sheets get equipment checkboxes; toggling gear recalculates carried weight, encumbrance level, Move, Dodge, skill levels, and melee to-hit/Parry instantly in the browser (5-level answer key baked at build; no per-toggle server call). Move/Dodge update everywhere they appear (Attributes, status line, Combat defense chips, readout as current/base). State persists to Cloudflare KV via a new `/api/loadout` Pages Function (reuses the `INBOX` namespace under a `loadout:` prefix, partitioned per player) with a localStorage cache + Reset; degrades gracefully with no KV. Pure recalc helpers (`gurps-calc.js`) mirror the `gurps_calc.py` oracle. Backbone (`applyModifiers` pipeline + reserved `hp`/`fp` KV fields) seeds SP2/SP3. Spec: `docs/superpowers/specs/2026-07-13-gurps-live-loadout-sp1-design.md`. plugin v1.8.23, publish tool 1.11.2 (PR #102)
- ~~GURPS live HP/FP tracking + condition penalties (SP2)~~ — Published GURPS PC sheets get editable current HP/FP in a persistent status panel above the tabs; editing derives Reeling (HP<⅓ ⇒ halve Move/Dodge) and Tired (FP<⅓ ⇒ halve Move/Dodge/ST), rounded up, cumulative (halve twice when both — RAW, RAG-confirmed), no cascade to ST-based quantities (Basic Lift/encumbrance/damage). Rides SP1's applyModifiers pipeline (new reeling/tired steps) and the reserved hp/fp KV state; new blocks/status-panel.js replaces the twice-rendered inline pips. Oracle helpers mirrored in gurps_calc.py/gurps-calc.js. Spec: docs/superpowers/specs/2026-07-15-gurps-live-hpfp-sp2-design.md. plugin v1.8.24, publish tool 1.11.3 (PR #103)
- ~~Upgraded GURPS character sheet on website~~ — Rebuilt the publish tool's GURPS PC renderer as a complete **Phoenix-style** record (GCA4 descendant of Armin's Color Flow Sheet): a new `tools/publish/lib/templates/gurps/` module (modular `blocks/` renderers + `layout.js`/`derived.js`/`parse.js`) replacing the thin renderer. 2-column block flow (attributes → defenses → hit-location DR → senses → encumbrance ramp → reactions → advantages/disads → skills → techniques → spells → points → melee/ranged → grimoire), Phoenix category zebra colors + `#0000FF` link-blue data text + `[ ]` point-cost brackets, a dedicated **Combat tab** (status banner, defenses, attack tables, action chains, rules-reference appendix) and **Equipment tab** (inventory + per-load-out tables). Spec: `docs/superpowers/specs/2026-06-28-publish-site-gurps-sheet-redesign-design.md`. plugin v1.7.7. Refined since: Base/Current skill columns (1.8.11–1.8.12), Techniques level resolution (1.7.1), current-encumbrance highlight (1.5.1).
- ~~Pathfinder 2e (Remaster) system support~~ — `systems/pf2e/` on the D&D model, sourced entirely from the consolidated ORC dataset (`~/PROJECTS/reference-srds/pf2e-orc-dataset/data/`, superseding the raw pf2orc mirror named in the original item): core five files (session-procedures carries the DC/encounter-XP/treasure GM math), spells.md → cantrips + rank 1-10 shards (408 Player Core/PC2 spells, index↔shard verified), monsters.md → six level-band shards (243 curated Monster Core creatures, compact 4-line statblocks), feats.md → general+skill/ancestry/class/archetype shards, all 24 ORC-remastered classes (magus/summoner/kineticist out of scope pending Remaster), ancestries/heritages/backgrounds, all 43 conditions, Player Core equipment. Routing (ttrpg-expert SKILL.md/INDEX.md, session-play), `pf2e` in the publish system enum, tone row, pc-pf2e.md template, publish tool 1.8.0 pc-pf2e.js renderer + tests, PF2e benchmark questions, ORC notice in ATTRIBUTION.md, gitignored `personal/`. All prose paraphrased — verified by a 10-word-shingle overlap audit against the dataset. plugin v1.8.15 (PR #81)
- ~~GURPS sheet calculation verification (`gurps_check.py`) SP1+SP2~~ — SP1 Tier 1 arithmetic: Basic Lift, encumbrance table, carried load vs declared Enc, Move/Dodge, secondary characteristics, Parry/Block, point budget, and the thrust/swing damage chain via closed-form B16/B269 arithmetic validated against GCS-generated oracle vectors (dev-time only). plugin v1.8.10 (PR #74). SP2 skill-level verification: B170 closed form (points + difficulty → relative level, WARNING on mismatch), Base residuals and naive-Current tripwires as INFO with the reconciliation pass owning perks/Talents (Armor Familiarity MA49 canonical); Skills table gains Base/Current columns (migration 1.8.11 → 1.8.12); publish tool 1.6.1 renders Current with base alongside. plugin v1.8.12
- ~~Extract remaining conditional reference chunks~~ — Measured per chunk as the item required. the-midwife got the extraction, and a bigger one than scoped: not just Worldbuilding Mode (1.8 KB → `references/worldbuilding-mode.md`) but Phase 4 Scaffold & Handoff + the Adventure Brief Template (8.2 KB → `references/scaffold-handoff.md`), cutting SKILL.md 22.3 KB → 13.0 KB (−42%) for the Discover/Shape/Structure turns that dominate midwife use. The other two chunks measured below the line and were skipped: reconcile 6.5's offer runs on *every* reconcile (even the Quick Scan fast path jumps to it) — only the ~1 KB acceptance body is conditional, and splitting it across files risks missed `world_evolved` bookkeeping for ~160 net tokens; session-prep 10b/10c are 0.8/1.0 KB behind *different* structure guards, each netting ~100–160 tokens only when its structure is absent. plugin v1.8.8 (PR #72)
- ~~Session-loop efficiency utilities~~ — Attacks the top two pain points by cost×time×frequency (measured 2026-07-04): `session_context.py` bundles session-prep's standard read-set into one call (the weekly prep gather was ~60k tokens / 37 tool calls in the saved C4 baseline); `stamp_entities.py` batch-stamps the wrap-up PC refresh (dry-run default, surgical frontmatter edits); `vault_check.py changed --since N` + a documented incremental Canon Audit mode scale monthly audit cost with the session delta instead of vault size (full sweep was 140k tokens on a 705-note vault). Wired into session-prep Context Source, session-wrapup 3c, and campaign-qa check procedures; new prep fixture + 17 regression checks. plugin v1.8.5 (PR #69)
- ~~Obsidian MCP stack removal + bundled vault utilities~~ — The archived MCP server stack (MCP Tools + Local REST API plugins, `search_vault` tool detection) removed from every skill, doc, and the README. Replaced with two bundled stdlib-Python utilities in `skills/shared/scripts/`: `graph_check.py` (orphans/unresolved/dead-ends/backlinks with full wikilink normalization) and `vault_search.py` (index-free BM25 ranked search). The Obsidian CLI was evaluated as the replacement and rejected on benchmark evidence — A/B on Canticle (3 query pairs) showed the CLI slower and costlier than grep for search and backlinks, and the utilities then beat both approaches on every axis: identical results in under a second and a few hundred tokens vs 50–125s and ~40–56k tokens per query. `shared/filesystem-mode.md` rewritten and renamed to `shared/vault-access.md`, the Vault Access Reference (filesystem + utilities; Obsidian is a viewer, never a dependency); campaign-qa Graph Health prefers the utility; fixture regression tests wired into CI; README gains a utilities section and an MCP migration note. Follow-up in the same release: `vault_check.py` (schema validation, name similarity, index drift, stale drafts) with a shared `schema_rules.py` extracted from the dev validator, `graph_check.py ambiguous`, validation-loop wiring in the three entity-creating skills, and a Python 3 one-time check in the quickstart (Windows note included). plugin v1.8.4 (PR #68)
- ~~canon_status standardization~~ — Three field names for canon status had coexisted since day one (`canon_status` in the original entity template, `source_confidence` blessed by the April QA pass, bare `confidence` from the entity-schema Universal Fields table), with three divergent reader chains in the publish tool and CI actively enforcing the wrong name. Standardized on `canon_status` everywhere: all templates/skills/validator/CI write and require it, publish tool reads it first (legacy names honored at read time), "confidence" naming purged from identifiers/UI/prose (`shared/canon-status.md`, `getCanonStatus`), fixed a SUPERSEDED-leak and missing-draft-badge bug, and shipped migration 1.7.10→1.8.0: a duplicate-safe vault-wide sweep on next skill use, plus a permanent campaign-qa Legacy Canon Field Repair check. tool 1.4.0, plugin v1.8.0 (PR #TBD)
- ~~publish-site Story section (N1–N6)~~ — The "make the site tell the STORY" North Star from `PUBLISH_SITE_BUGS_SPEC.md`. A prose-first Story reading layer over existing vault narrative, alongside the unchanged Wiki: a `/story.html` landing with two branches — the **Campaign Saga** (dedicated story pages from each unit's `Narrative Recap`, adaptive chapter-or-session spine, prev/next cover-to-cover, no dead pages) and **Character Stories** (per-PC `*_Story.md`, grouped Current/Retired/Fallen; PC stat-sheet tab links to it). Recaps found by heading wherever they live (separate wrap-up *or* embedded session/chapter file), matched even when decorated, paired by **folder proximity** (robust to free-form refs), built from the gm-only-stripped published view (no spoiler leak), and omitted entirely when a vault has no narrative. New `lib/story-spine.js` + `story.js`/`story-landing.js` templates. Validated across three real vaults (Canticle 14 + 7 char pages, specialforces 8, space_game graceful-empty). tool 1.3.2, plugin v1.7.10 (PR #TBD)
- ~~publish-site rendering defects (B1–B8 batch 1)~~ — Six defects from `PUBLISH_SITE_BUGS_SPEC.md`, each reproduced against main then fixed with tests: **B8** exclude lists from `vault-config.md` and `vault.config.json` now union instead of shadow (a `Source References` spoiler leak); **B6** "NPCs in Play" + relationship graph derive from a stripped "published view" not raw markdown (gm-only/excluded names leaked); **B3** cross-subtree links 404'd from a file-as-dir `relativePath` misuse, fixed with a shared `relativeHref` helper (11 call sites in wiki/location/npc); **B1** wikilinks humanize underscore slugs; **B4** the 404 page gets its genre theme; **B2** sparse sidebars collapse to one wide column. B7 (stale PC sheets) was already shipped by #57/#58 and skipped. tool 1.3.1, plugin v1.7.9 (PR #TBD)
- ~~publish-site install/upgrade hardening~~ — Two bugs broke a live site on plugin upgrade (1.7.0→1.7.7). (1) The build tool shipped without its runtime deps: distributed by git-copy into the plugin cache and symlinked via a `file:` pin, Node resolved its `require()`s from the cache where `npm install` never runs, so `gray-matter`/`lunr`/`markdown-it` were missing. Fixed by **vendoring** prod deps (committed; root `.gitignore` negation) so a clean install builds offline with no manual steps. (2) Stale version pins failed silently — a bare `/plugin` update never repointed sites. Added build-time **version-drift detection** (`lib/version-check.js`) that warns with the exact `file:` fix, tightened routine-update repoint + verification, and documented the hard-pin tradeoff. Also **committed fully to the plugin-cache distribution model**: `init` auto-pins new sites to the cache version it ran from (scaffold default `"latest"` → self-referential `file:` pin), and the SKILL/wizard/README stop pointing users at the stale npm registry. Guardrails: `clean-install` + `runtime-deps` + init auto-pin tests; tool 1.3.0, plugin v1.7.8 (PR #TBD)
- ~~Gotchas: non-Earth date-rule fix~~ — Extracted the one benchmark-proven win from the shelved "gotchas consolidation": the false rule that `in_game_date` "must be parseable by JS `new Date()`" made compliant agents *fabricate* a Gregorian date for fantasy/sci-fi calendars (e.g. "14th of Flamerule, 1492 DR" → "July 14, 1492"). Corrected in session-wrapup, vault-ingest, and shared/session-document-chain to record non-Earth dates in the world's own format (v1.7.6). The broader "rules stated up front" consolidation was **shelved** — A/B benchmark showed no behavioural payoff on Opus 4.8 or Sonnet; full work preserved on branch `archive/vault-write-gotchas`
- ~~Narrative planning layer~~ — New `plan` entity type with `plan_type` discriminator (arc/scene/investigation/timeline). Plans live in `Planning/` under each chapter; the-midwife Phase 4 promotes them alongside entities; session-prep surfaces relevant plans. Six skills updated (PR #53)
- ~~Wire PC `## Current Status` consumers~~ — The cumulative PC status block (PR #57) was read by nothing but the website + GM. Wired four consumers — session-prep (Threads + arc check, fixing thread-decay), the-midwife (new-chapter hooks), ttrpg-expert (arc/thread routes), campaign-qa (Canon Audit consistency check) — with the read contract in the two ttrpg-expert references and a structural regression test in CI (PR #58)
- ~~session-wrapup: PC entity-sheet drift fix~~ — Wrap-up never refreshed the PC's own sheet, so its `asOfSession`, chapter `tags`, and the published `## Current Status` froze behind the Story file and overview. New Step 3c refreshes active PC sheets each session; `## Current Status` is now a canonical template section; added a `validate_schema.py freshness` regression check + test fixture (PR #57)
- ~~World evolution integration into reconcile~~ — Reconcile step 5.5 offers faction turns, consequence surfacing, foreshadowing review after session confidence is promoted. Removed dead campaign-tracker.md references. Added `world-evolution` entity source and `world_evolved` session field (PR #52)
- ~~Publish tool: landing recap + wrap-up sidebar fix~~ — Landing page now extracts recap from wrap-up file, links to wrap-up page, wrap-up pages suppress backlinks sidebar, world_flags excluded from build (PR #51)
- ~~Campaign overview template with current game date~~ — `campaign_overview` entity type with session-wrapup auto-updates and the-midwife creation
- ~~the-midwife: guided adventure creation skill~~ — Adventure creation skill with adventure-brief entity type, vault-mining, Session 0 handoff
- ~~Publish tool: site redesign v1.2~~ — Navigation overhaul, breadcrumbs, genre presets, system-specific PC sheets (CoC/GURPS/D&D/FitD), lunr search, relationship graphs, timeline pages, context sidebars, index page redesigns, landing page rewrite (PR #40)
- ~~Publish tool: GURPS PC sheet — single alphabetized skills list~~ — Inline comments enforcing single alphabetized tables for Skills and Spells (PR #32)
- ~~Publish tool: mobile HTML fixes~~ — Accordion table horizontal scroll, back-to-top button with scroll-triggered fade (PR #32)
- ~~Publish tool: displayTitle + template overhaul~~ — displayTitle on all page objects, data-driven `display_meta` PC meta row, Team/Fallen landing split with SVG status icons (PR #25)
- ~~Publish tool: Event dedicated template~~ — Dedicated event template, vault template, session-lifecycle event decomposition (PR #20)
- ~~Publish tool: Landing page dashboard~~ — Hero, session recap, PC roster, key NPCs, explore grid
- ~~Publish tool: Landing page timeline snippet~~ — Visual timeline of recent events on the landing page
- ~~Publish tool: overhaul site navigation~~ — Breadcrumbs, entity-type grouping, cross-linking, mobile responsiveness
- ~~Publish tool: system-specific PC sheet rendering~~ — Per-system HTML/CSS treatments (CoC parchment, GURPS stat blocks, D&D ability cards, FitD)
- ~~Publish tool: Timeline auto-generation~~ — Timeline page generated from event/session dates in frontmatter
- ~~Publish tool: Full-text client-side search (lunr)~~ — Client-side search index for published site
- ~~Publish tool: Relationship graph visualisation~~ — Static SVG relationship graphs with force-directed layout
- ~~Publish tool v1.0~~ — npm package + skill for vault-to-static-site generation (PR #19)
- ~~Session-prep: GM approval gate for new scenes~~ — GM framing questions before scene creation
- ~~Entity creation must read template before writing~~ — Template-reading gate in campaign-organizer, session-wrapup, vault-ingest
- ~~Remove model-specific prescriptions from all skills~~ — Replaced hardcoded model names with complexity guidance (PRs #26, #27)
- ~~Skill roles audit~~ — Skill taxonomy table in README, clarified advisor-only boundaries (PR #27)
- ~~FitD SRD enrichment (Phase 5b)~~ — Copyright compliance, 3 new SRD files, depth pass, personal reference build-out, skill routing
- ~~CoC/BRP SRD enrichment (Phase 5a)~~ — Terminology fixes, 18 content files, Regency variant overlay
- ~~Regency Cthulhu system support~~ — 5 overlay files in variants/regency/, routing updates
- ~~GURPS content restructure (Phase 4)~~ — 31 files, 7 chargen kits
- ~~Copyright compliance (Phases 1-3)~~ — All systems
- ~~Codebase QA pass~~ — Score 11.0 (Impact 3, Urgency 5, Effort S)
- ~~Filesystem fallback (campaign-qa, session-lifecycle)~~ — PR #14
- ~~Shared-refs extraction~~ — Merged to main

## Notes

- **Pulp Cthulhu** is a CoC 7e variant adding pulp-era heroics:
  hero/pulp talents, psychic powers, archetypes, adjusted lethality.
  Implement as CoC variant overlay (similar to Regency Cthulhu).

- **Phase 5 order** (confirmed): CoC/BRP → FitD → D&D.
  Each system is a discrete unit — complete one before starting
  the next.

- **GURPS enrichment** is not on the list — already done in
  Phase 4 (24 topic files, 7 chargen kits).
