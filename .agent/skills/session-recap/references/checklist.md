# Checklist (run before calling RECAP done)

- [ ] Gate query pasted, shows a real date, not `(pending)` or absent.
- [ ] `vault/episodes/<NNN>/sNN-recap.md`: every claim traces to a page the `ingest(sNN)` commit
      touched or the transcript directly supporting one.
- [ ] `vault/episodes/<NNN>/sNN-recap.md`: 900–1350 words; hook by sentence two.
- [ ] `vault/episodes/<NNN>/sNN-recap.md`'s closing line hands off to the next
      session's run-guide Recap box and its opening moment — not a self-contained ending.
- [ ] `vault/episodes/<NNN>/sNN-highlights.md`: 3–8 quotes, 1–3 moments, every one L-number cited,
      verbatim (not paraphrased).
- [ ] Every quote/moment passed the in-world filter.
- [ ] Every named entity wikilinked on first mention in both files;
      unresolved links flagged, not invented.
- [ ] `created`/`updated` are today's real date, not the template's `"{date}"` placeholder.
- [ ] Both files `status: pending`, `publish: false` — neither touched.
- [ ] `vault/episodes/<NNN>/recap-draft.md` deleted if one was promoted; untouched if none
      existed.
- [ ] No other `vault/` page, `vault/campaigns/shattered-sea/pcs/`, or
      repo-root `CLAUDE.md` touched.
- [ ] `PUBLISH-PROPOSED:` line stated in the response, not written to disk.
- [ ] `RECAP:` marker pasted; commit made as `recap(sNN): <slug>`.
- [ ] `retrospective.md`, if requested: DM's own words, no `publish:` key,
      never generated unprompted.
